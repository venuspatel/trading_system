# ── Layer 1: Market Data Ingestion ───────────────────────────
alpaca-py>=0.20.0       # Alpaca Markets (active provider)
pandas>=2.0.0           # DataFrame support throughout the agent
python-dotenv>=1.0.0    # Load .env files

# ── Layer 1: Future providers (install when needed) ───────────
# ib_insync>=0.9.86     # Interactive Brokers — uncomment when ready
# python-binance>=1.0.19 # Binance — uncomment when ready

# ── Coming in later layers ────────────────────────────────────
# pandas-ta>=0.3.14b    # Layer 2: Technical indicators
# vectorbt>=0.25.0      # Layer 3: Backtesting
# scikit-learn>=1.3.0   # Layer 4: Strategy selection ML
